<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.linwei.cams.module.login">

    <application
        android:name="com.linwei.cams.component.common.base.CommonBaseApplication"
        android:allowBackup="true"
        android:icon="@mipmap/login_ic_launcher"
        android:label="@string/login_app_name"
        android:supportsRtl="true"
        android:theme="@style/LoginModuleAppTheme">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/login_module_page">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>